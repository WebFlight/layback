
var parser = {
	/**
	 * Sum 2 int's
	 * @param  {int} a int
	 * @param  {int} b int
	 * @return {int}   Sum of ints
	 */
	sum: function(a, b) {
		var sum = a + b;
		return sum;
	}
};

module.exports = parser;
